General Infos:
==============
project = 52North SensorThingsAPI Reference Implementation (${artifactId})
version = ${project.version}
builddate = ${git.build.time}

Git Infos:
=================
repository = ${git.remote.origin.url}
path = ${git.branch}
revision = ${git.commit.id.full}
lastCommitMessage = ${git.commit.message.short}
lastCommitDate = ${git.commit.time}
