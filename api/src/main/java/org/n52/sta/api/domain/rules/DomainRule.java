package org.n52.sta.api.domain.rules;

import org.n52.sta.api.domain.aggregate.EntityAggregate;
import org.n52.sta.api.entity.Identifiable;

public interface DomainRule /*<T extends Identifiable, A extends EntityAggregate<T>> */ {

    // void assertSave(A aggregate) throws DomainRuleViolationException;

    // void assertUpdate(A aggregate) throws DomainRuleViolationException;

    // void assertDelete(A aggregate) throws DomainRuleViolationException;
}
